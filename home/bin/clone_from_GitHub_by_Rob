#!/usr/bin/env bash
# Intended to be executed in a Bash shell.
[[ -n "${BO_Trace}" ]] && 1>&2 echo "Executing ${BASH_SOURCE}" && [[ "${BO_Trace}" != 'TRACE' ]] && set -vx
# NO: set -e
trap warn_on_error EXIT
###############################################################################
# Clone git repositories, if they have not been cloned already.
# NOTE: I reference my special SSH configuration to handle the connection, see `~/.ssh/config`.
# TODO: Implement this:
# hint:   git config pull.ff only       # fast-forward only

source ${HOME}/bin/lib/declare-git.bash

_Source=GitHub
_Connection=MeAt${_Source}
_Dir=${HOME}/repo/${_Source}

_Profile=rdw20170120
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" workstation

main() {
    # TODO: DESCRIPTION

    # TODO: CONTENT

    return 0
}

main $@

###############################################################################
# NOTE: Uncomment these lines for debugging, placed where needed
# export PS4='$ ' ; set -vx
# Code to debug...
# set +vx

: << 'DisabledContent'
_Profile=rdw20170120
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" astech
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" aws-inventory
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" cb-infrastructure
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" cert
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" couchbase
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" euler
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" first
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" hortonworks
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" javascript
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" ocdp
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" potrero
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" quoting-api
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" restinjava
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" sample-python
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" samplegwtviaant
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" samples
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" trading
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" training
maybe_clone "${_Dir}/wip" "${_Connection}:${_Profile}" workstation
DisabledContent

