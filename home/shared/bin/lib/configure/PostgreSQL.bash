#!/usr/bin/env false
################################################################################
# Configure PostgreSQL

export PGDATA=${HOME}/.local/var/pgsql/data

# Create a new database:
# mkdir -p ${PGDATA}
# initdb
# pg_ctl start
# pg_ctl stop

: << 'DisabledContent'
DisabledContent

