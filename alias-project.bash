#!/bin/false
# NOTE: Intended to be sourced into a BASH shell

# BASH alias definitions specific to this project

alias cycle='clear ; test-run && generate && app-run'
alias list-sort='sort -nr --key=5'

